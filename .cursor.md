🎯 목표: TypeLegal의 모든 백엔드 및 프론트엔드 테스트가 CI 환경에서 안정적으로 통과되도록 테스트 코드를 단계별로 리팩토링합니다.

---

## ✅ 테스트 환경 분리 및 복구 지시사항 (2025-04-21 기준)

### 📌 공통

- 테스트 개선 중이므로, CI/CD 파이프라인에 테스트 통과 조건 임시 제외 또는 `--passWithNoTests` 사용
- `Playwright` 테스트는 `Jest`와 분리하여 실행
- 테스트 설정 관련 파일:
  - `jest.config.js`
  - `jest.setup.js`
  - `playwright.config.ts`

---

### 🧪 프론트엔드

#### 1. 환경 설정

- `frontend/package.json` 기준 다음 devDependencies 설치 완료 필요:

```bash
npm install --save-dev jest-environment-jsdom identity-obj-proxy babel-jest @testing-library/jest-dom
```

- `jest.config.js` 파일 설정:

```ts
module.exports = {
	testEnvironment: 'jsdom',
	setupFilesAfterEnv: ['<rootDir>/jest.setup.js'],
	transform: {
		'^.+\\.(js|jsx|ts|tsx)$': ['babel-jest', { presets: ['next/babel'] }],
	},
	moduleNameMapper: {
		'\\.(css|less|scss|sass)$': 'identity-obj-proxy',
		'\\.(jpg|jpeg|png|svg|webp|gif|ttf|woff|woff2)$': '<rootDir>/__mocks__/fileMock.js',
	},
	transformIgnorePatterns: ['/node_modules/', '^.+\\.module\\.(css|sass|scss)$'],
};
```

- `jest.setup.js` 모킹 구성 예시:

```js
import '@testing-library/jest-dom';
window.document.body = document.createElement('body');
window.document.addEventListener = jest.fn();
window.addEventListener = jest.fn();
```

- `__mocks__/fileMock.js`:

```js
module.exports = 'test-file-stub';
```

#### 2. Playwright 테스트 분리

- Jest 실행 시 `tests/e2e/` 제외되도록 설정 필요
- 별도 실행:

```bash
npx playwright test
```

---

### 🔧 백엔드

#### 1. 컴파일 오류 수정 (2025-04-21 기준)

- `ClauseResponseDto`가 setter 없는 불변 객체로 변경됨 → 테스트코드에서 builder 사용 또는 생성자 대체 필요
- `ClauseService.getFilteredClauses()` → 인자 개수 불일치 수정 필요 (기존 1개 → 2개로 변경됨)

#### 2. bindingCidx 관련

- 프론트에서만 사용되는 필드이므로 엔티티 내 `@Transient` 처리 고려 또는 DTO로 분리

#### 3. 테스트 실행 명령어

```bash
cd backend
./mvnw test
```

---

## 🗂️ 참고 파일

- backend_test_log_raw.txt
- frontend_test_log_raw.txt
- test_results_stats.json
- test_summary.md

---

⚠️ 주의사항

- 절대 메인 함수(Service, Controller, Entity 등 애플리케이션 실제 로직)는 수정하지 말고, **오직 테스트 코드만 수정**합니다.
- 모든 테스트는 실제 실행해보고 실패 → 수정 → 재실행을 반복하여 **통과 상태를 스스로 확인**한 뒤 진행합니다.
- 테스트 내에서 수정한 부분은 왜 그렇게 했는지에 대한 **간단한 주석을 각 테스트 함수 내에 추가**해주세요.
- 테스트 코드에 어떤 상태나 조건이 필요한데 불확실한 경우, **절대 임의로 판단하지 말고 저에게 질문**해주세요.

🚫 `--updateSnapshot`은 사용하지 마세요. 스냅샷 생성은 변경 의도가 명확한 경우에만 허용합니다.

📦 결과물

- 백엔드: `mvnw test` 전체 통과
- 프론트엔드: `npm run test` 전체 통과
- 커밋 로그: `test: fix ...` 형식의 세분화된 기록
